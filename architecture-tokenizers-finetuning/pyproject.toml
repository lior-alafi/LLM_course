[project]
name = "ex2"
version = "0.1.0"
description = "Exercise 2"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "transformers",
    "huggingface-hub",
    "accelerate",
    "torch",
    "datasets",
    "peft",
    "trl",
    "python-dotenv"
]
